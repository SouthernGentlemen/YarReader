# YarReader v0.7.0

USB/tablet portability correction from source commit `3cd292c`: the reader is
HTML-first, and a validated active generation can be materialized as a real
directory rather than copied as a workstation symlink.

Validated with 40 synthetic tests at the milestone.
